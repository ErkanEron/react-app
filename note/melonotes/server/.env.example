# Server Configuration
PORT=8080
NODE_ENV=production

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here

# Database Configuration (for production you might want to use a cloud database)
DB_PATH=/app/melonotes.db

# CORS Origins (comma separated)
CORS_ORIGINS=https://your-netlify-domain.netlify.app,http://localhost:3000